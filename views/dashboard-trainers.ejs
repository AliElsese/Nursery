<%- include('include/_headerAdmin') %>

        <div class="content">
            <div class="product-table m-5">
                <div class="col-md-12 text-center mt-5 pt-5">
                    <a href="/add-trainer" class="get-started-btn">اضافة مدرس</a>
                </div>

                <% if(typeof message != 'undefined') { %>
                    <div class="col-md-12 text-center">
                        <h4 class="alert alert-danger mt-4">
                            <%= message %>
                        </h4>
                    </div>
                <% } %>

                <form action="/views/dashboard-trainers" method="post">
                
                    <table class="table table-striped table-bordered text-center mydatatable" style="width: 100%;">
                
                        <thead style="background-color: #2f323a;color: #fff;">
                            <tr>
                                <th>رقم التسلسل</th>
                                <th>اسم المدرس</th>
                                <th>التخصص</th>
                                <th>الفصل</th>
                                <th>اسم المستخدم</th>
                                <th>كلمة المرور</th>
                                <th>تحكم</th>
                            </tr>
                        </thead>
        
                        <tbody>
                            <%- include('include/_show-trainers') %>
                        </tbody>
        
                    </table>

                </form>
    
            </div>
        </div>

<%- include('include/_footerAdmin') %>